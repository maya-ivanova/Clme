name: .NET CI

on: 
  push:
    branches: ["main", "mihome", "miwork"]
  pull_request:
    branches: ["main", "mihome", "miwork"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      sqlserver:
        image: mcr.microsoft.com/mssql/server:2022-latest
        env:
          ACCEPT_EULA: 'Y'
          MSSQL_SA_PASSWORD: ${{ secrets.AC_SHOP_DB_PW }}
        ports:
          - 1433:1433

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0'

      - name: Restore
        run: dotnet restore Coolme.slnx

      - name: Build
        run: dotnet build Coolme.slnx --configuration Release --no-restore

      - name: Run Tests
        env:
          # This connects your code to the Docker container above
          ConnectionStrings__DefaultConnection: "Server=localhost;Database=ACShopDb;User Id=sa;Password=${{ secrets.AC_SHOP_DB_PW }};TrustServerCertificate=True"
        # Running against the solution file ensures all test projects are found
        run: dotnet test Coolme.slnx --configuration Release --no-build --verbosity normal